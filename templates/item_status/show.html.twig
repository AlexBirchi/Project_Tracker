{% extends 'base.html.twig' %}

{% block title %}ItemStatus{% endblock %}

{% block body %}
    <h1>ItemStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_item_status_index') }}">back to list</a>

    <a href="{{ path('app_item_status_edit', {'id': item_status.id}) }}">edit</a>

    {{ include('item_status/_delete_form.html.twig') }}
{% endblock %}
